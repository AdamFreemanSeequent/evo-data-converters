[project]
name = "evo-data-converters-adamf"
version = "0.0.1"
description = "Python data converters for ADAMF files to Evo geoscience objects."
readme = "README.md"
authors = [
    { name = "Seequent", email = "support@seequent.com" }
]
requires-python = ">=3.10"
dependencies = [
    "evo-data-converters-common",
    "evo-schemas>=2025.12.15",
    "numpy>=1.19.0",
    "pandas",
    "pyarrow>=21.0.0",
]

[project.urls]
Source = "https://github.com/SeequentEvo/evo-data-converters"
Tracker = "https://github.com/SeequentEvo/evo-data-converters/issues"
Homepage = "https://www.seequent.com/"
Documentation = "https://developer.seequent.com/"

[dependency-groups]
dev = [
    "pytest"
]

[tool.ruff]
src = ["src", "tests"]
line-length = 120

[project.scripts]
evo-data-converters-adamf = "evo_data_converters_adamf:main"

[build-system]
requires = ["hatchling", "hatch-fancy-pypi-readme"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
include = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/evo"]

[tool.uv.sources]
evo-data-converters-common = { workspace = true }

[tool.hatch.metadata.hooks.fancy-pypi-readme]
content-type = "text/markdown"

[[tool.hatch.metadata.hooks.fancy-pypi-readme.fragments]]
path = "README.md"

[[tool.hatch.metadata.hooks.fancy-pypi-readme.substitutions]]
# Literal TOML strings (single quotes) need no escaping of backslashes.
# Converts relative links to absolute links in PyPI
pattern = '\[(.+?)\]\(((?!https?://)\S+?)\)'
replacement = '[\1](https://github.com/SeequentEvo/evo-data-converters/tree/main/packages/adamf/\g<2>)'
